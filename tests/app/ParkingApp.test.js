const tk = require("timekeeper");
const { ParkingApp } = require("../../src/app/ParkingApp");
const { Address } = require("../../src/models/Address");
const { FeeModel } = require("../../src/models/FeeModel");

const assert = require("chai").assert;

describe("ParkingApp -> createParkingLot, park, unPark for given examples", () => {
  describe("Example 1: Small motorcycle/scooter parking lot", () => {
    
  });
});
