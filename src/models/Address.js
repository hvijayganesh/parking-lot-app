class Address {
  constructor(street, city, state, pinCode) {
    this.street = street;
    this.city = city;
    this.state = state;
    this.pinCode = pinCode;
  }
}

module.exports = {
  Address,
};
